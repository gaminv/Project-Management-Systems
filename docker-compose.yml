services:
  client:
    build:
      context: ./client
      dockerfile: Dockerfile
    ports:
      - '5173:80'
    depends_on:
      - server
    restart: always

  server:
    build:
      context: ./server
      dockerfile: Dockerfile
    ports:
      - '4000:8080'
    restart: always
